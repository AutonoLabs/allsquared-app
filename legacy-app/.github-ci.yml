# Moved to .github/workflows/ci.yml (September 2026).
# GitHub Actions only runs workflows under .github/workflows/.
#
# - checker/  → always runs (test + build)
# - legacy-app/ → path-filtered frozen job (typecheck, test, build)
# - CodeQL    → .github/workflows/codeql.yml
